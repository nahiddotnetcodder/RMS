@model RMS.Models.HREmpRoaster
@{
    ViewData["Title"] = "Human Resource (HR) > Operations > Employee Roaster > Edit Employee Roster";
}
<h6 style="color: #1275AE; margin-top: 10px;">@ViewData["Title"]</h6>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="HRERId" />
            <div class="form-group">
                <label asp-for="HREDId" class="control-label"></label>
                <select asp-for="HREDId" class="form-control" asp-items="ViewBag.HREmpDetails"></select>
                <span asp-validation-for="HREDId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HRERDate" class="control-label"></label>
                <input asp-for="HRERDate" class="form-control" />
                <span asp-validation-for="HRERDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="IsPresent" type="checkbox" />
                <label asp-for="IsPresent" class="control-label"></label>
                <span asp-validation-for="IsPresent" class="text-danger"></span>
            </div>
            <div class="form-group btn-group">
                <input type="submit" value="Update" class="btn btn-primary" />
                <a class="btn btn-secondary" asp-action="index"
                   asp-route-pg="@TempData.Peek("CurrentPage")">Back</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
