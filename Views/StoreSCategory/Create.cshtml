@model RMS.Models.StoreSCategory
@{
    ViewData["Title"] = "Store & Inventory > Maintenance > Sub Category > Create Sub Category";
}
<h6 style="color: #1275AE; margin-top: 10px;">@ViewData["Title"]</h6>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SCId" class="control-label"></label>
                <select asp-for="SCId" class="form-control" asp-items="ViewBag.StoreCategory"></select>
                <span asp-validation-for="SCId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SSCName" class="control-label"></label>
                <input asp-for="SSCName" class="form-control" />
                <span asp-validation-for="SSCName" class="text-danger"></span>
            </div>
            <div class="form-group btn-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a class="btn btn-secondary" asp-action="index"
                   asp-route-pg="@TempData.Peek("CurrentPage")">Back</a>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
